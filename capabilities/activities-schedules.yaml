naftiko: 1.0.0-alpha2
info:
  label: Airbnb Activities API — Schedules
  description: 'Airbnb Activities API — Schedules. 4 operations. Lead operation: List Experience Schedules. Self-contained
    Naftiko capability covering one Airbnb business surface.'
  tags:
  - Airbnb
  - Schedules
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    AIRBNB_API_KEY: AIRBNB_API_KEY
capability:
  consumes:
  - type: http
    namespace: activities-schedules
    baseUri: https://api.airbnb.com/v2
    description: Airbnb Activities API — Schedules business capability. Self-contained, no shared references.
    resources:
    - name: experiences-experience_id-schedules
      path: /experiences/{experience_id}/schedules
      operations:
      - name: listexperienceschedules
        method: GET
        description: List Experience Schedules
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: start_date
          in: query
          type: string
          description: The start date for the schedule range in ISO 8601 format.
          required: true
        - name: end_date
          in: query
          type: string
          description: The end date for the schedule range in ISO 8601 format.
          required: true
      - name: createexperienceschedule
        method: POST
        description: Create an Experience Schedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: experiences-experience_id-schedules-schedule_id
      path: /experiences/{experience_id}/schedules/{schedule_id}
      operations:
      - name: updateexperienceschedule
        method: PUT
        description: Update an Experience Schedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deleteexperienceschedule
        method: DELETE
        description: Delete an Experience Schedule
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.AIRBNB_API_KEY}}'
  exposes:
  - type: rest
    namespace: activities-schedules-rest
    port: 8080
    description: REST adapter for Airbnb Activities API — Schedules. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/experiences/{experience-id}/schedules
      name: experiences-experience-id-schedules
      description: REST surface for experiences-experience_id-schedules.
      operations:
      - method: GET
        name: listexperienceschedules
        description: List Experience Schedules
        call: activities-schedules.listexperienceschedules
        with:
          start_date: rest.start_date
          end_date: rest.end_date
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createexperienceschedule
        description: Create an Experience Schedule
        call: activities-schedules.createexperienceschedule
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/experiences/{experience-id}/schedules/{schedule-id}
      name: experiences-experience-id-schedules-schedule-id
      description: REST surface for experiences-experience_id-schedules-schedule_id.
      operations:
      - method: PUT
        name: updateexperienceschedule
        description: Update an Experience Schedule
        call: activities-schedules.updateexperienceschedule
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteexperienceschedule
        description: Delete an Experience Schedule
        call: activities-schedules.deleteexperienceschedule
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: activities-schedules-mcp
    port: 9090
    transport: http
    description: MCP adapter for Airbnb Activities API — Schedules. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: list-experience-schedules
      description: List Experience Schedules
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: activities-schedules.listexperienceschedules
      with:
        start_date: tools.start_date
        end_date: tools.end_date
      outputParameters:
      - type: object
        mapping: $.
    - name: create-experience-schedule
      description: Create an Experience Schedule
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: activities-schedules.createexperienceschedule
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: update-experience-schedule
      description: Update an Experience Schedule
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: activities-schedules.updateexperienceschedule
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-experience-schedule
      description: Delete an Experience Schedule
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: activities-schedules.deleteexperienceschedule
      outputParameters:
      - type: object
        mapping: $.
